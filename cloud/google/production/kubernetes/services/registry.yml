# Kubernetes declaration of Identifiers.org Prefix Registration API service
# Author: Manuel Bernal Llinares <mbdebian@gmail.com>
apiVersion: v1
kind: Service
metadata:
    name: registry
    labels:
        environment: production
        tier: api
        app: registry
