# Kubernetes deployment description for Identifiers.org Resource Recommender Web Service
#
# Author: Manuel Bernal Llinares <mbdebian@gmail.com>
apiVersion: apps/v1beta2
kind: Deployment
metadata:
    name: resource-recommender
spec:
    selector:
        matchLabels:
            environment: production
            tier: api
            app: resource-recommender
    replicas: 1
    template:
        metadata:
            labels:
                environment: production
                tier: api
                app: resource-recommender
        spec:
            containers:
                - name: cloud-ws-resource-recommender
                  image: identifiersorg/cloud-ws-resource-recommender:0.1.1
                  imagePullPolicy: Always
                  resources:
                      requests:
                          memory: 640Mi
                  ports:
                  - containerPort: 8083
